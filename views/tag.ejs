<!-- Tag Hero Banner -->
<section class="dev-hero" style="padding: 2.5rem 0 2rem;">
  <div class="dev-hero__body">
    <div class="dev-hero__badge">
      <span class="dev-hero__badge-dot"></span>
      <span>Tag Explorer</span>
    </div>

    <h1 class="dev-hero__heading" style="font-size: clamp(1.8rem, 3.5vw, 2.8rem);">
      #<%= tag.name %>
    </h1>
    <p class="dev-hero__sub">
      Explore all articles tagged with <strong>#<%= tag.name %></strong> on DevHub.
    </p>
  </div>
</section>

<!-- Articles Grid -->
<section class="articles">
  <h2 class="articles__heading">
    <%= data.length %> Article<%= data.length !== 1 ? 's' : '' %> Found
  </h2>

  <% if (data.length === 0) { %>
    <div style="background: var(--bg-surface); border: 1px dashed var(--border-light); padding: 3rem; border-radius: var(--radius-lg); text-align: center; color: var(--text-muted);">
      No articles found with this tag yet.
    </div>
  <% } else { %>
    <ul class="article-grid">
      <% data.forEach(post => { %>
        <li class="article-card">
          <div>
            <% if (post.featuredImage) { %>
              <img src="<%= post.featuredImage %>" alt="<%= post.title %>" class="article-card__cover">
            <% } %>

            <h3 class="article-card__title">
              <a href="/post/<%= post._id %>"><%= post.title %></a>
            </h3>

            <p style="color: var(--text-muted); font-size: 0.95rem; line-height: 1.5; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; margin-top: 0.5rem;">
              <%= post.body %>
            </p>

            <% if (post.tags && post.tags.length > 0) { %>
              <div style="display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: 0.8rem;">
                <% post.tags.forEach(t => { %>
                  <a href="/tag/<%= t.slug %>" class="tech-badge" style="text-decoration: none; font-size: 0.75rem; padding: 0.2rem 0.6rem; <%= t.slug === tag.slug ? 'background: var(--primary-indigo); color: white;' : '' %>">
                    #<%= t.name %>
                  </a>
                <% }) %>
              </div>
            <% } %>
          </div>

          <div class="article-card__meta">
            <span><%= post.createdAtFormatted %></span>
            <span>•</span>
            <span>⏱ <%= post.readingTime %></span>
            <% if (post.category) { %>
              <span class="article-card__badge" style="margin-left: auto;"><%= post.category.name %></span>
            <% } %>
          </div>
        </li>
      <% }) %>
    </ul>
  <% } %>

  <% if (nextPage !== null) { %>
    <div style="text-align: center; margin-top: 2rem;">
      <a href="/tag/<%= tag.slug %>?page=<%= nextPage %>" class="pagination">&lt; Older Articles</a>
    </div>
  <% } %>
</section>
